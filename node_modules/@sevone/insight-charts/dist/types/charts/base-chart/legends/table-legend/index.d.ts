import * as React from 'react';
import { LegendDataType, Row, Column } from '../types';
declare type Props = {
    rows: Array<Row>;
    columns: Array<Column>;
    toggleData?: boolean;
    legendData: LegendDataType;
    onRowClick?: (ids: Array<string>) => void;
    onColumnReorder?: (order: Array<string>, parent: string) => void;
    onColumnResize?: (column: string, width: string | number) => void;
};
export default class TableLegend extends React.Component<Props> {
    static defaultProps: {
        toggleData: boolean;
    };
    handleLegendClick: (ids: string[]) => void;
    generateRows: () => Row[];
    generateColumns: () => Column[];
    generateSelectedRows: () => string[];
    handleColumnReorder: (order: string[], parent: string) => void;
    handleColumnResize: (column: string, width: React.ReactText) => void;
    render(): JSX.Element;
}
export {};

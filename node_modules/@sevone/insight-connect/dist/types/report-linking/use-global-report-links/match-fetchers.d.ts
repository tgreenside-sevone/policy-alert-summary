import { ReportLinkFilterType, LinkableResourceArrayType } from './types';
declare type MatchFetcherType = (datasourceId: number, payloads: LinkableResourceArrayType, filters: ReportLinkFilterType[]) => Promise<LinkableResourceArrayType | null>;
/**
 * Fetch functions to query for resources that match the filter on the
 * report link. Returning [] means nothing matched, returning null
 * means there was no filter, meaning that everything matches.
 */
export declare const matchFetchers: Record<string, MatchFetcherType>;
export {};

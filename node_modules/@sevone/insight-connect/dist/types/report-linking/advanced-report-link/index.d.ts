import * as React from 'react';
import { LocationType } from '../../navigation/types';
import { ReportLinkPayload } from '../report-linking';
declare type Props = {
    /** The ids of the available reports to link to */
    reports: Array<string | number>;
    /** The data that will be sent in the link via url parameters */
    payload: ReportLinkPayload;
    /** Whether the menu is visible or not */
    visible: boolean;
    /** Horizontal offset of the menu relative to the bottom left */
    offsetX?: number;
    /** Vertical offset of the menu relative to the bottom left */
    offsetY?: number;
    /** Class to add to your target wrapper */
    className?: string;
    /** Styles to add to your target wrapper */
    style?: React.CSSProperties;
    /** Custom menu component to display the available report links */
    menu?: React.ComponentType<{
        links: Array<{
            label: string;
            location: LocationType;
        }>;
    }>;
    /** Triggered when a user clicks on the wrapped element */
    onClick?: (event: React.MouseEvent) => void;
    /** Triggered when a user performs an action to change the menu visibility */
    onVisibilityChange: (visible: boolean) => void;
    /** The component you want linkable */
    children: React.ReactNode;
};
declare function AdvancedReportLink(props: Props): JSX.Element;
export { AdvancedReportLink };

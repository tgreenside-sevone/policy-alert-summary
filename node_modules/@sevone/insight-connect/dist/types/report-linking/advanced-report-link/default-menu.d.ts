import { LocationType } from '../../navigation/types';
declare type Props = {
    links: Array<{
        label: string;
        location: LocationType;
    }>;
};
declare function DefaultMenu(props: Props): JSX.Element;
export { DefaultMenu };

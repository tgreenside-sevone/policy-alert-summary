import * as React from 'react';
import { RequestInstance } from '../request/request';
import { HistoryInstance } from '../navigation/history';
import { ThemeType } from '../theme/types';
declare type Props = {
    /** The path of your app that pages and links are relative to */
    basePath: string;
    /** The full url to Data Insight's server */
    serverUrl: string;
    /** The token used to authenticate the current user */
    authToken: string | null;
    theme: ThemeType;
    children: React.ReactNode;
};
declare type State = {
    request: RequestInstance | null;
    history: HistoryInstance | null;
};
declare class InsightProvider extends React.Component<Props, State> {
    state: State;
    componentDidMount(): void;
    componentDidUpdate(prevProps: Props): void;
    updateRequestObject(): void;
    updateHistoryObject(): void;
    render(): JSX.Element | null;
}
export { InsightProvider };

import { DeviceTagType } from '../types';
declare type Props = {
    label?: string;
    isMulti?: boolean;
    maxDisplayed?: number;
    /** Whether the current selection can be cleared */
    clearable?: boolean;
    disabled?: boolean;
    datasources: Array<number | null>;
    value: Array<DeviceTagType>;
    deviceTagOptions?: Array<DeviceTagType>;
    limit?: number;
    placeholder: string;
    query: string;
    cacheKey: 'deviceGroup' | 'deviceType';
    pathsFilterKey: 'paths' | 'paths2';
    onChange: (value: Array<DeviceTagType>) => void;
};
declare function DeviceTagSelector(props: Props): JSX.Element;
export { DeviceTagSelector };

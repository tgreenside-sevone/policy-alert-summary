import { DeviceGroupType } from '../types';
declare type Props = {
    label?: string;
    isMulti?: boolean;
    maxDisplayed?: number;
    /** Whether the current selection can be cleared */
    clearable?: boolean;
    disabled?: boolean;
    datasources: Array<number | null>;
    value: Array<DeviceGroupType>;
    deviceGroupOptions?: Array<DeviceGroupType>;
    limit?: number;
    onChange: (value: Array<DeviceGroupType>) => void;
};
declare function DeviceGroupSelector(props: Props): JSX.Element;
export { DeviceGroupSelector };

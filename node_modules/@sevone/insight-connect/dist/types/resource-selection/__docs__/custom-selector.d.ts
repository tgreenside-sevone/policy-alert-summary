import * as React from 'react';
import { CustomSelectorType } from '../types';
declare type SelectorType = CustomSelectorType<{
    value: string;
}>;
declare type Props = React.ComponentProps<SelectorType['selector']>;
declare function CustomSelector(props: Props): JSX.Element;
export { CustomSelector };

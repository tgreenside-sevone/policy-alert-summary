import * as React from 'react';
import { ResourceType, HierarchicalDataType, CustomSelectorType } from './types';
import { PathsType } from './selection-paths';
declare type Props = {
    isMultiDatasource?: boolean;
    selectableDatasource?: boolean;
    datasource: number | Array<number> | null;
    value: ResourceType | null;
    hierarchicalData: HierarchicalDataType;
    allowedPaths?: PathsType;
    customTypes?: Array<CustomSelectorType>;
    addons?: Array<React.ReactNode>;
    onChange: (datasource: number | Array<number> | null, value: ResourceType | null, hierarchicalData: HierarchicalDataType) => void;
};
declare function ResourceSelection(props: Props): JSX.Element;
export { ResourceSelection };

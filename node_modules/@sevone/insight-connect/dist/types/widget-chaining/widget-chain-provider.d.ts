import * as React from 'react';
import { WidgetChainType } from './types';
declare type Props = {
    widgetId: string;
    chains: Array<WidgetChainType> | null;
    children: React.ReactNode;
};
declare function WidgetChainProvider(props: Props): JSX.Element;
export { WidgetChainProvider };

import { UserSettingsType } from './types';
declare type UseUserSettingsType = {
    settings: UserSettingsType | null;
    isFetchingSettings: boolean;
};
declare function useUserSettings(): UseUserSettingsType;
export { useUserSettings };

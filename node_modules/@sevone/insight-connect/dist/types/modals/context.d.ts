import * as React from 'react';
import { ModalType } from './types';
export declare type InternalModalType = {
    id: string;
    modal: React.ReactNode;
};
export declare type ModalContextType = {
    showModal: (modal: ModalType) => string;
    modals: Array<InternalModalType>;
};
declare const ModalContext: React.Context<ModalContextType>;
export { ModalContext };

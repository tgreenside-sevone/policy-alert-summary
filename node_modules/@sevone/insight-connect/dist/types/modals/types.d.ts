/// <reference types="react" />
declare type ModalProps = {
    hideModal: () => void;
};
export declare type BasicModalType = {
    type: 'error' | 'warning' | 'info' | 'confirmation';
    header: string;
    message: (props: ModalProps) => React.ReactNode;
    actions?: (props: ModalProps) => Array<React.ReactNode>;
};
export declare type AdvancedModalType = React.ComponentType<ModalProps>;
export declare type ModalType = BasicModalType | AdvancedModalType;
export declare type ModalApiType = {
    showModal: (modal: ModalType) => string;
};
export {};

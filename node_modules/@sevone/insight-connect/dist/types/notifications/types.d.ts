/// <reference types="react" />
export declare type NotificationType = {
    type: 'error' | 'warning' | 'info' | 'success';
    lifespan?: number | null;
    message: React.ReactNode;
    actions?: Array<{
        label: string;
        onClick: (event: React.MouseEvent, dismiss: () => void) => (void | Promise<any>);
    }>;
    onDismiss?: () => void;
    onClose?: () => void;
};
export declare type NotificationApiType = {
    showNotification: (notification: NotificationType) => string;
};

import * as React from 'react';
import { NotificationType } from './types';
export declare type InternalNotificationType = {
    id: string;
    createdAt: number;
} & NotificationType;
export declare type NotificationContextType = {
    showNotification: (notification: NotificationType) => string;
    notifications: Array<InternalNotificationType>;
};
declare const NotificationContext: React.Context<NotificationContextType>;
export { NotificationContext };

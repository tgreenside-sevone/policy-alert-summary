import * as React from 'react';
import { NotificationContextType } from './context';
import { NotificationType } from './types';
declare type Props = {
    children: React.ReactNode;
};
declare type State = NotificationContextType;
declare class NotificationProvider extends React.Component<Props, State> {
    constructor(props: Props);
    showNotification: (notification: NotificationType) => string;
    handleHideNotification: (id: string) => void;
    render(): JSX.Element;
}
export { NotificationProvider };

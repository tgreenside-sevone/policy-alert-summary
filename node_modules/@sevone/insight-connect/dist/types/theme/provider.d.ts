import * as React from 'react';
import { ThemeType } from './types';
declare type Props = {
    theme: ThemeType;
};
declare class ThemeProvider extends React.PureComponent<Props> {
    generateCssVariables(): string;
    render(): JSX.Element;
}
export { ThemeProvider };

import { getHistory } from './history';
import { LocationType } from './types';
declare type RawLocationType = ReturnType<typeof getHistory>['location'];
declare type TransformedLocationType = {
    pathname: string;
    hash: string;
    params: {
        [key: string]: any;
    };
    state: {
        breadcrumbs?: Array<{
            label: string;
            location: string | LocationType;
        }>;
        [key: string]: any;
    };
};
declare function transformLocation(location: RawLocationType): TransformedLocationType;
export { transformLocation };

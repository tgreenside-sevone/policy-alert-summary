import { NavigationType } from './types';
declare type MatchType = {
    match: {
        params: {
            [key: string]: any;
        };
    };
};
declare function useNavigation(): NavigationType & MatchType;
export { useNavigation };

declare type ConfigureType = {
    api?: {
        sevone: {
            server?: string;
            authToken?: string;
        };
    };
    assets?: {
        server?: string;
    };
};
declare const configure: (options: ConfigureType) => void;
export default configure;

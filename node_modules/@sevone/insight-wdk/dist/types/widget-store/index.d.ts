import { FacetSchema } from '../facet-manager/types';
declare type WidgetDataType = {
    name: string;
    version: string;
    title: string;
    description: string;
    producedFacets?: Array<FacetSchema>;
    consumedFacets?: Array<FacetSchema>;
    runtime: string;
};
declare function registerWidget(data: WidgetDataType): void;
declare function getWidgets(): string[];
declare function getWidget(name: string): WidgetDataType;
export { registerWidget, getWidgets, getWidget };

import * as React from 'react';
declare type Props = {
    /** Label of badge */
    label?: string;
    /** Type of badge you are displaying */
    type?: 'default' | 'error' | 'warning' | 'info';
    /** Classname to add to the target wrapper */
    className?: string;
    /** The position of the indicator badge */
    position?: 'topLeft' | 'topRight';
    /** The target component to attach the indicator badge to */
    children?: React.ReactNode;
};
declare function Badge(props: Props): JSX.Element;
export { Badge };

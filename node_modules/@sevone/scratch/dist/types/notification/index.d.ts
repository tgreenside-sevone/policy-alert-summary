import * as React from 'react';
declare type Props = {
    /** The type of message you are displaying */
    type: 'error' | 'warning' | 'info' | 'success';
    /** The content of the notification */
    children: React.ReactNode;
    /** Actions to display along with the notification */
    actions?: Array<{
        label: string;
        onClick: (event: React.MouseEvent, dismiss: () => void) => (void | Promise<any>);
    }>;
    /** Triggered when the close button is clicked */
    onClose: () => void;
};
declare class Notification extends React.PureComponent<Props> {
    render(): JSX.Element;
}
export { Notification };

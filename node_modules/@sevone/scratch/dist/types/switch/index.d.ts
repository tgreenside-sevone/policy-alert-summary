import * as React from 'react';
declare type Props = {
    /** Whether it is checked or not */
    checked: boolean;
    /** Whether it is disabled or not */
    disabled?: boolean;
    /** The label */
    children?: React.ReactNode;
    /** Triggered when the user changes the check value */
    onChange: (checked: boolean) => void;
};
declare class Switch extends React.PureComponent<Props> {
    handleClick: () => void;
    render(): JSX.Element;
}
export { Switch };

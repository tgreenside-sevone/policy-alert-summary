import * as React from 'react';
declare type Props = {
    /** Unix timestamp in milliseconds. Can be two values to indicate a range */
    selectedDates?: Array<number>;
    /** Unix timestamps of disabled dates */
    disabledDates?: Array<number>;
    /** Unix timestamps of marked dates */
    markedDates?: Array<number>;
    /** The month (1 to 12) and year pair to display */
    page: {
        month: number;
        year: number;
    };
    /** Trigger when the user selects a date */
    onDateSelection?: (dates: Array<number>) => void;
    /** Triggered when the user moves between months and years */
    onPageChange: (page: {
        month: number;
        year: number;
    }) => void;
};
declare class Calendar extends React.PureComponent<Props> {
    static defaultProps: {
        selectedDates: number[];
        disabledDates: number[];
        markedDates: number[];
    };
    handleDateSelection: (date: Date) => void;
    render(): JSX.Element;
}
export { Calendar };

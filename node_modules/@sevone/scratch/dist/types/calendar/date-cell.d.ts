import * as React from 'react';
declare type Props = {
    activeMonth: number;
    selectedDates: Array<Date>;
    disabledDates: Array<Date>;
    markedDates: Array<Date>;
    day: number;
    month: number;
    year: number;
    onDateSelection: (date: Date) => void;
};
declare class DateCell extends React.PureComponent<Props> {
    isWithinRange(date: Date, range: Array<Date>): boolean;
    isSelectedDate(date: Date, selectedDates: Array<Date>): boolean;
    isDisabledDate(date: Date, disabledDates: Array<Date>): boolean;
    handleDateSelection: () => void;
    render(): JSX.Element;
}
export default DateCell;

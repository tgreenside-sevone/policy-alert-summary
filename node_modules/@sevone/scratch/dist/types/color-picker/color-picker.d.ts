import * as React from 'react';
declare type Props = {
    /** String representation of the current color. */
    value: string;
    /** Pre-selected colors to select from. */
    swatches?: Array<string>;
    /** Triggered when the user modifies the color. */
    onChange: (color: string) => void;
};
declare class ColorPicker extends React.PureComponent<Props> {
    handleChange: (value: any) => void;
    handleHueChange: (hue: number) => void;
    handleAlphaChange: (alpha: number) => void;
    render(): JSX.Element;
}
export default ColorPicker;

import * as React from 'react';
declare type Props = {
    /** The value when this radio option is selected */
    value: string | number;
    /** Disable this option */
    disabled?: boolean;
    /** The label to appear next to the radio */
    children: React.ReactNode;
};
declare class Radio extends React.PureComponent<Props> {
    handleChange: (onChange: (value: string | number) => void) => void;
    render(): JSX.Element;
}
export default Radio;

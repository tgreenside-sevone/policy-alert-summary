import * as React from 'react';
declare type Props = {
    /** Unique value to identify the tab */
    id: string;
    /** Tab text */
    title: string;
    /** Whether the title can be edited inline */
    editable?: boolean;
    /** Icon to show alongside the tab title */
    icon?: React.ReactNode;
    /** Class to add to the tab content wrapper */
    className?: string;
    /** Content shown when the tab is selected */
    children: React.ReactNode;
    /** Triggered when a user edits a tab's title */
    onChange?: (id: string, title: string) => void;
};
declare class Tab extends React.PureComponent<Props> {
    render(): JSX.Element;
}
export default Tab;

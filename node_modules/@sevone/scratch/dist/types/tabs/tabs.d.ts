import * as React from 'react';
import Tab from './tab';
import { TabIdType } from './types';
declare type TabProps = React.ComponentProps<typeof Tab>;
declare type Props = {
    /** The selected tab id */
    selectedTab?: TabIdType | null;
    /** Whether the tabs are displayed vertically */
    vertical?: boolean;
    /** The tabs and their content */
    children: Array<React.ReactElement<TabProps>>;
    /** Triggered when a user tries to add a new tab */
    onAdd?: () => void;
    /** Triggered when a user selects a new tab */
    onSelect: (id: TabIdType) => void;
    /** Triggered when a user reorders the tabs */
    onReorder?: (order: Array<TabIdType>) => void;
};
declare class Tabs extends React.PureComponent<Props> {
    static defaultProps: {
        vertical: boolean;
    };
    handleSelect: (id: string | number) => void;
    render(): JSX.Element;
}
export default Tabs;

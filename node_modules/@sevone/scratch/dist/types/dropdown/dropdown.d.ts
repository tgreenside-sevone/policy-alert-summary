import * as React from 'react';
export declare type Props = {
    /** Whether the menu is visible or not */
    visible: boolean;
    /** Which mouse actions should trigger visibility changes */
    trigger: Array<'click' | 'hover'>;
    /** Where the menu should position itself relative to the dropdown */
    position?: 'bottomLeft' | 'bottomRight' | 'left' | 'right' | 'topLeft' | 'topRight';
    /** The menu displayed when visible */
    menu: React.ReactNode;
    /** The tag used to wrap your dropdown component */
    renderDropdownTag?: React.ReactType;
    /** A classname to append to the dropdown's wrapper */
    renderDropdownClassname?: string;
    /** The element that acts as the dropdown trigger */
    children: React.ReactNode;
    /** Triggered when a user performs an action to change the menu visiblity */
    onVisiblityChange: (shown: boolean) => void;
};
declare class Dropdown extends React.PureComponent<Props> {
    static defaultProps: {
        position: string;
        renderDropdownTag: string;
        renderDropdownClassname: string;
    };
    menuRef: React.RefObject<HTMLElement>;
    handleVisibilityChange: (visible: boolean) => void;
    renderMenu: (props: {
        ref: React.RefObject<HTMLDivElement>;
    }) => JSX.Element;
    render(): JSX.Element;
}
export default Dropdown;

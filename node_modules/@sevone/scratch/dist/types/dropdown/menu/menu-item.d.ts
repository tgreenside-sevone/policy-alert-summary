import * as React from 'react';
declare type Props = {
    /** Whether the item is disabled or not */
    disabled?: boolean;
    /** The content of the item */
    children: React.ReactNode;
    /** Triggered when the user clicks on the menu item */
    onClick?: (event: React.MouseEvent) => (void | Promise<any>);
};
declare class DropdownItem extends React.PureComponent<Props> {
    handleClick: (event: React.MouseEvent<Element, MouseEvent>) => void;
    render(): JSX.Element;
}
export default DropdownItem;

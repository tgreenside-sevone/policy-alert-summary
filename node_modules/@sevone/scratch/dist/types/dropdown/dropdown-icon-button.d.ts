import * as React from 'react';
import { Cancelable } from '../utils/cancelable';
import { Props as DropdownProps } from './dropdown';
declare type Props = {
    /** Whether the buttons are disabled or not */
    disabled?: boolean;
    /** Triggered when the user clicks the secondary button */
    onClick?: () => (void | Promise<any>);
} & DropdownProps;
declare type State = {
    pendingClick: Cancelable | null;
};
declare class DropdownIconButton extends React.Component<Props, State> {
    state: State;
    componentWillUnmount(): void;
    handleClickHandler: (onClick: () => void | Promise<any>) => void;
    handleClick: () => void;
    handleDisabledTriggerClick: (e: React.MouseEvent<Element, MouseEvent>) => void;
    renderMenu: (menu: React.ReactNode) => React.ReactNode;
    render(): JSX.Element;
}
export { DropdownIconButton };

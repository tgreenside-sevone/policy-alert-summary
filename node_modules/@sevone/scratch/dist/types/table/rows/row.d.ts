import * as React from 'react';
import { RowType, ColumnType } from '../types';
declare type Props = {
    data: RowType;
    columns: Array<ColumnType>;
    pinned: boolean;
    selected: boolean;
    selectable: boolean;
};
declare class Row extends React.PureComponent<Props> {
    render(): JSX.Element;
}
export default Row;

import * as React from 'react';
declare type Props = {
    /** The content of the edit field */
    value: string;
    /** Whether the field can be edited or not */
    disabled?: boolean;
    /** Whether to allow the user to create multiple lines with the `Enter` key */
    multiLine?: boolean;
    /** Triggered when a user loses focus on the field */
    onBlur?: (value: string) => void;
    /** Triggered when a user changes the field's value */
    onChange?: (value: string) => void;
};
declare type State = {
    active: boolean;
};
declare class InlineEdit extends React.PureComponent<Props, State> {
    state: {
        active: boolean;
    };
    static defaultProps: {
        value: string;
        disabled: boolean;
        multiLine: boolean;
        onBlur: () => void;
        onChange: () => void;
    };
    handleBlur: (value: string) => void;
    handleFocus: () => void;
    render(): JSX.Element;
}
export { InlineEdit };

import * as React from 'react';
import ListItem from './list-item';
declare type IdType = string | number;
declare type ListItemProps = React.ComponentProps<typeof ListItem>;
declare type Props = {
    /** Whether to allow multiple selections or not */
    isMulti?: boolean;
    /** The ids of selected items */
    selected: Array<IdType>;
    /** Whether to render only the rows visible in the viewport */
    virtualScroll?: boolean;
    /** The items for selection */
    children: Array<React.ReactElement<ListItemProps>>;
    /** Triggered when a user selects an item */
    onChange: (selected: Array<IdType>) => void;
    /** Triggered by react-virtuoso when the inner list height changed. Useful for adjusting scrollbar */
    onTotalListHeightChanged?: (height: number) => void;
};
/**
 * RawSelectableList exposes some virtual scroll options from react-virtuoso for
 * internal use in the Table that we don't (yet) want to expose to scratch users.
 */
declare class RawSelectableList extends React.PureComponent<Props> {
    getAllIds: () => (string | number)[];
    handleSelection: (id: string | number, event: React.MouseEvent<Element, MouseEvent>) => void;
    handleSelectAll: () => void;
    render(): JSX.Element | JSX.Element[];
}
export default RawSelectableList;

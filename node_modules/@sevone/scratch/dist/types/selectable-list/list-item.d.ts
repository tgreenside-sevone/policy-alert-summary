/**
 * This is just a dummy component for users to use in their lists. We intercept
 * all the list's children though and replace them with `ProxyItem`. This is
 * because we only need users to supply `id` and `children` props, but we need
 * a few more to actually generate each item. If users directly used the
 * `ProxyItem` component, they'd see the other props we need and think they
 * need to supply them themselves.
 */
import * as React from 'react';
declare type RenderProps = {
    isSelected: boolean;
};
declare type Props = {
    /** Unique value to identify the item */
    id: string | number;
    /** Function to render item */
    children: (props: RenderProps) => React.ReactNode;
    /** Extra props you want passed to your render function */
    [key: string]: any;
};
declare class ListItem extends React.PureComponent<Props> {
}
export default ListItem;

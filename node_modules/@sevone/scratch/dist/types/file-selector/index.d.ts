import * as React from 'react';
declare type Props = {
    /** The selected files */
    files: Array<File>;
    /** Whether or not to allow multiple files */
    multiple?: boolean;
    /** The types of files allowed to be selected. Empty is unrestricted. */
    accept?: Array<string>;
    /** The element to trigger file selection when clicked */
    children: React.ReactNode;
    /** Triggered when there is a change in the selected files */
    onChange: (files: Array<File>) => void;
};
declare class FileSelector extends React.PureComponent<Props> {
    fileInput: {
        current: null | HTMLInputElement;
    };
    static defaultProps: {
        multiple: boolean;
    };
    constructor(props: Props);
    componentDidUpdate(): void;
    handleClick: () => void;
    handleFileInputChange: () => void;
    handleFileDrop: (event: React.DragEvent<Element>) => void;
    handleFileChange: (files: File[]) => void;
    render(): JSX.Element;
}
export { FileSelector };

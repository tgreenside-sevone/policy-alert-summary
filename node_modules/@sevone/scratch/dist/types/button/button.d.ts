import * as React from 'react';
export declare type Props = {
    /** Whether the button is disabled or not */
    disabled?: boolean;
    /** Secondary button style */
    secondary?: boolean;
    /** Whether the button will take up the full width of its container  */
    fullWidth?: boolean;
    /** The display style of the button */
    type?: 'contained' | 'outlined' | 'minimal';
    /** Element placed before the children */
    prefixIcon?: React.ReactNode;
    /** Element placed after the children */
    suffixIcon?: React.ReactNode;
    /** Handler for the click event */
    onClick: (event: React.MouseEvent) => (void | Promise<any>);
    /** Content of the button */
    children: React.ReactNode;
};
declare class Button extends React.PureComponent<Props> {
    static defaultProps: {
        disabled: boolean;
        secondary: boolean;
        type: string;
    };
    render(): JSX.Element;
}
export default Button;

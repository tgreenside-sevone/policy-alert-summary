import * as React from 'react';
declare type Props = {
    /** The title to appear along the top */
    title?: React.ReactNode;
    /** List of action items that appear along the bottom  */
    actions?: Array<React.ReactNode>;
    children: React.ReactNode;
};
declare class Card extends React.PureComponent<Props> {
    static defaultProps: {
        type: string;
    };
    render(): JSX.Element;
}
export { Card };

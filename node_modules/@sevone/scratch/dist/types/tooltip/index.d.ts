import * as React from 'react';
declare type primaryPositions = 'top' | 'right' | 'bottom' | 'left';
declare type secondaryPositions = 'top' | 'right' | 'bottom' | 'left' | 'center';
declare type Props = {
    /** Where to display the tooltip, relative to the trigger element */
    position: [primaryPositions, secondaryPositions];
    /** The content of the tooltip */
    tooltip: React.ReactNode;
    /** The amount of time, in milliseconds, to wait to show the tooltip */
    triggerDelay?: number;
    /** Class to add to your element wrapper */
    className?: string;
    /** The element to trigger the tooltip when hovered */
    children: React.ReactNode;
};
declare type State = {
    isVisible: boolean;
};
declare class Tooltip extends React.PureComponent<Props, State> {
    state: {
        isVisible: boolean;
    };
    handleVisiblityChange: (isVisible: boolean) => void;
    render(): JSX.Element;
}
export { Tooltip };

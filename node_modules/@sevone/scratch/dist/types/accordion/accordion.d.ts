import * as React from 'react';
import AccordionSection from './accordion-section';
declare type Props = {
    /** The id of sections that should be expanded */
    expanded: string | Array<string> | null | undefined;
    /** The sections and their content */
    children: Array<React.ReactElement<typeof AccordionSection>>;
    /** Triggered when a user expands a section */
    onExpand: (id: string) => void;
    /** Triggered when a user collapses a section */
    onCollapse: (id: string) => void;
};
declare class Accordion extends React.PureComponent<Props> {
    render(): JSX.Element;
}
export default Accordion;

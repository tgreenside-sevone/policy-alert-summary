import * as React from 'react';
declare type Props = {
    /** Display loading bar as an infinitely loading animation */
    infinite?: boolean;
    /** The current progress of the loading bar */
    progress?: number | null | undefined;
    /** Special effect to indicate status */
    status?: 'error' | 'warning' | 'info';
};
declare class LoadingBar extends React.PureComponent<Props> {
    static defaultProps: {
        infinite: boolean;
        progress: number;
        status: string;
    };
    render(): JSX.Element;
}
export { LoadingBar };

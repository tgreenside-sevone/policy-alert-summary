import * as React from 'react';
import MenuItem from '../menu-item';
declare type Props = {
    /** Unique value to identify the menu item */
    id: string;
    /** Icon to display */
    icon?: React.ReactNode;
    /** Menu text */
    title: React.ReactNode;
    /** Its menu items */
    children: Array<React.ReactElement<typeof MenuItem> | React.ReactElement<typeof SubMenu>>;
};
declare type State = {
    isExpanded: boolean;
};
declare class SubMenu extends React.PureComponent<Props, State> {
    state: {
        isExpanded: boolean;
    };
    handleExpansionChange: (isExpanded: boolean) => void;
    render(): JSX.Element;
}
export default SubMenu;
